PROJECT PURPOSE
A full-stack email scheduling system where users can log in, upload recipients, schedule emails, and monitor scheduled and sent emails.

TECHNOLOGY STACK

Frontend:
- React
- TypeScript
- Tailwind CSS

Backend:
- Node.js
- Express.js
- TypeScript
- Prisma

Infrastructure:
- PostgreSQL
- Redis
- BullMQ
- Elasticsearch
- Docker

Integrations:
- Google OAuth
- Slack OAuth
- Ethereal Email / Nodemailer

COMPLETE EMAIL SCHEDULING FLOW

User
→ Google Login
→ Dashboard
→ Compose New Email
→ Upload CSV/TXT recipients
→ Set subject, body, start time, delay and hourly limit
→ Frontend calls backend API
→ Backend validates request
→ Email jobs stored in PostgreSQL
→ Idempotency key generated
→ Jobs added to BullMQ/Redis as delayed jobs
→ Scheduled email indexed in Elasticsearch
→ BullMQ waits until scheduled time
→ Worker picks up job
→ Redis-backed hourly rate limit checked
→ If limit available:
    → Email sent through Ethereal SMTP
    → Database updated to SENT
    → Elasticsearch index updated
→ If rate limit exceeded:
    → Job is delayed/rescheduled to next available window
    → Slack notification sent if Slack is connected

DATABASE RESPONSIBILITIES

PostgreSQL is the source of truth.

Store:
- Users
- Senders
- Email jobs
- Status
- Scheduled time
- Sent time
- Idempotency keys
- Slack connection details

REDIS RESPONSIBILITIES

Redis is used for:
- BullMQ job storage
- Delayed job scheduling
- Distributed rate-limit counters
- Coordination that works across multiple workers/instances

BULLMQ WORKFLOW

- API creates delayed jobs
- Redis persists jobs
- Worker processes jobs
- Configurable concurrency
- Minimum delay between sends
- Jobs survive backend restarts
- Idempotency prevents duplicate sends

RATE LIMITING FLOW

Worker receives job
→ Check Redis counter using sender + current hour
→ Limit available?
    YES → Increment counter → Send email
    NO → Do not drop job
          → Calculate next available time
          → Reschedule/delay job
          → Notify Slack if connected

Limits are configurable through environment variables.

ELASTICSEARCH FLOW

PostgreSQL is the source of truth
→ Important email fields are indexed in Elasticsearch
→ Dashboard search API queries Elasticsearch
→ Users can search scheduled and sent emails by recipient, subject, or other indexed fields.

SERVER RESTART FLOW

Server stops
→ PostgreSQL data remains persisted
→ Redis/BullMQ jobs remain persisted
→ Docker volumes preserve infrastructure data
→ Server/worker starts again
→ BullMQ continues processing delayed jobs
→ Previously sent jobs are not resent because of database status and idempotency checks.

SLACK NOTIFICATION FLOW

User clicks Connect Slack
→ Slack OAuth authorization
→ Backend receives OAuth callback
→ Slack token/connection stored securely
→ Rate limit reached
→ Backend sends real Slack API notification
→ If Slack is not connected, scheduler continues without failing.

HIGH LOAD BEHAVIOR

System handles 1000+ emails scheduled around the same time:
- Jobs are persisted in Redis
- BullMQ queues the jobs
- Configurable worker concurrency controls parallel processing
- Minimum delay throttles sending
- Hourly rate limits prevent exceeding provider limits
- Excess jobs are rescheduled instead of dropped
- Database and idempotency checks prevent duplicate sends

FINAL SYSTEM ARCHITECTURE

                    ┌──────────────────┐
                    │ React Dashboard  │
                    └────────┬─────────┘
                             │ API
                    ┌────────▼─────────┐
                    │ Express Backend  │
                    └───┬────┬────┬────┘
                        │    │    │
          ┌─────────────┘    │    └──────────────┐
          ▼                  ▼                   ▼
     PostgreSQL            Redis           Elasticsearch
     Source of Truth       BullMQ Jobs      Search Index
                               │
                               ▼
                        BullMQ Worker
                               │
                    ┌──────────┴──────────┐
                    ▼                     ▼
              Ethereal SMTP           Slack API

MODULE 1 — Infrastructure
- [x] Express + TypeScript backend
- [x] PostgreSQL + Prisma
- [x] Redis infrastructure
- [x] Elasticsearch infrastructure
- [x] Docker Compose setup

MODULE 2 — Email Scheduling
- [x] EmailJob persistence
- [x] Scheduled email API (POST /api/emails/schedule & GET /api/emails)
- [x] BullMQ delayed jobs & Redis queue
- [x] Email worker with configurable concurrency
- [x] Idempotency using idempotencyKey

MODULE 3 — Real Email Sending with Nodemailer + SMTP
- [x] SMTP configuration via environment variables
- [x] Reusable Nodemailer transport module with Ethereal fallback
- [x] Real email sending integrated inside BullMQ worker
- [x] State transitions: SCHEDULED -> PROCESSING -> SENT
- [x] sentAt timestamp updated on delivery

MODULE 4 — Retry & Failure Handling
- [x] BullMQ retry attempts (`EMAIL_MAX_ATTEMPTS`)
- [x] Configurable max attempts & exponential backoff (`EMAIL_RETRY_DELAY`)
- [x] Database attempt tracking (`attempts` field in EmailJob)
- [x] Safe failure error handling (`lastError` sanitized message)
- [x] Retry lifecycle (SCHEDULED / RETRYING -> PROCESSING -> SENT or FAILED)
- [x] Final FAILED status when max attempts exhausted

MODULE 5 — Cancel Scheduled Emails
- [x] CANCELLED enum value added to EmailJobStatus in schema.prisma
- [x] Database migration applied (`add_cancelled_email_status`)
- [x] `cancelEmailJob` function in `email.service.ts` removing BullMQ job & updating DB
- [x] `DELETE /api/emails/:id` route & controller handler
- [x] Worker safety guard skipping CANCELLED jobs

MODULE 6 — Hourly Rate Limiting
- [x] Redis-backed distributed hourly counter per sender (`rate_limit:{senderId}:{yyyy-MM-dd-HH}`)
- [x] Configurable `MAX_EMAILS_PER_HOUR=10` in environment
- [x] Atomic Lua script (`eval`) preventing race conditions under worker concurrency (`EMAIL_WORKER_CONCURRENCY=5`)
- [x] Rate limit check prior to email send attempt
- [x] Automatic rescheduling to next hour window when quota is exceeded without marking job FAILED or consuming retry attempts
- [x] Independent rate limits per sender
- [x] Integration with cancellation and retry mechanics verified
- [x] TypeScript verification (npx tsc --noEmit: 0 errors)

MODULE 7 — Elasticsearch Indexing & Search API
- [x] `@elastic/elasticsearch` client module (`backend/src/lib/elasticsearch.ts`)
- [x] Index `email-jobs` with explicit mapping for subject, body, recipientEmail, status, timestamps
- [x] Elasticsearch service (`indexEmailJob`, `updateEmailJobInElasticsearch`, `searchEmailJobs`)
- [x] Non-blocking safe indexing on `POST /api/emails/schedule`
- [x] Document status synchronization across all state transitions (`SCHEDULED`, `PROCESSING`, `RETRYING`, `SENT`, `FAILED`, `CANCELLED`)
- [x] Search API (`GET /api/emails/search?q=<query>&status=<status>`) with full-text search and status filtering
- [x] Reindex script (`npm run es:index`)
- [x] Safe error handling when Elasticsearch is offline
- [x] Verified directly on raw Elasticsearch cluster and via REST endpoints

MODULE 8 — Observability & Metrics
- [x] Prometheus metrics module (`backend/src/lib/metrics.ts`) using `prom-client`
- [x] Standard process metrics (`collectDefaultMetrics`)
- [x] Custom Prometheus Counters: `emails_scheduled_total`, `emails_sent_total`, `emails_failed_total`, `emails_retried_total`, `emails_cancelled_total`, `emails_rate_limited_total`
- [x] Custom Prometheus Histogram: `email_processing_duration_seconds`
- [x] Custom Prometheus Gauges: `email_queue_waiting`, `email_queue_active`, `email_queue_delayed`
- [x] Metrics Endpoint: `GET /metrics` returning Prometheus text format (200 OK)
- [x] Workflow Integration in scheduling service & worker processing loop
- [x] Error isolation ensuring metrics failures never affect core email delivery
- [x] Automated test script (`npm run test:metrics`)

CURRENT PROJECT STATUS

Completed:
- Express + TypeScript backend
- PostgreSQL + Prisma
- Redis infrastructure
- Elasticsearch infrastructure & full-text search API
- Prometheus metrics & observability endpoint (`GET /metrics`)
- BullMQ email scheduling queue
- Real email delivery via Nodemailer + SMTP
- Retry & failure handling with exponential backoff
- Cancel scheduled emails endpoint & worker guard
- Distributed hourly rate limiting with atomic Lua script and job rescheduling

Next module:
Google OAuth & Authentication